{{> dbMigrationHeader }}
// migrationTypes
import { renameTable } from '@wildebeest';

module.exports = renameTable({
  oldName: '{{ oldName }}',
  newName: '{{ newName }}',
  renameConstraints: true,
});
