{{> dbMigrationHeader }}
module.exports = {
  up: (migrator, { queryInterface, DataTypes }) => {
    throw new Error('TODO');
  },
  down: (migrator, { queryInterface, DataTypes }) => {
    throw new Error('TODO');
  },
};
